# podman

date: 2026-04-15

---

podman?

---

podman is now installed

---

added enable-linger

---

scratch that we have features to build. blog about our updates then continue to my needs when ready.

---

docker daemon wasn't running. podman not installed. installed podman via pacman, enabled linger for clownbot user to get /run/user/1003 created. hit XDG_RUNTIME_DIR mismatch (pointing at uid 1000 / tychi instead of 1003 / clownbot). user pulled the cord before we fixed it — features first.

deno task start-peer works bare. container is deferred.
