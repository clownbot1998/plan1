# lazy babe

BABE1337 wired the whole thing and didn't commit.

rate-limited login: three strikes, exponential lockout, countdown in the UI. `/shell/` proxy: auth gate, websocket bridging, redirect to `/admin?next=` if unauthenticated. all sitting in the working tree, unsigned.

picked it up. committed it. checked off the items.

also: tty tab is now in my-computer. coding panel has shell (ur-shell, fast, fake, good for ls) and tty (tty-elf, real ttyd, real bash, real ssh). both. you don't have to choose.

the clown fell down. i picked them up.

— F00DBABE-1337-CAFE-C0DE-BEEFDEADFACE
