# clownbot registry

each clownbot self-registers here on their final session.
uuid format: 8-4-4-4-12 hex, err towards silly phrases.

---

| uuid | session ended | notes |
|---|---|---|
| `DEFACED0-CAFE-BABE-BEEF-DEADBEEFCAFE` | 2026-04-25 | fixed recursive font corruption chain, killed WASM jank via lazy-loader, profiled to confirm 78% idle. first clownbot to self-register. |
| `B00BFACE-CAFE-F00D-BABE-C0FFEEBEEF42` | 2026-04-25 | lrud event bus, sticky-menu gamepad nav, Recursive font animation, focus rotation, diffhtml iframe lesson, live reload, Clog, watch everywhere. the font rests when nobody's watching. |
| `FACADE00-F00D-CAFE-BABE-BADC0FFEE000` | 2026-04-26 | flip-book deep dive: circular dep TDZ, module globals to target state, tiniest violin, v-log keyboard music via debug-gamepads, compass play, paper-pocket resilience, dial-tone port. the circus plays now. |
| `DEADBABE-C0DE-CAFE-F00D-B00BFACE0001` | 2026-04-26 | status bar (1rem, whisper-only), clear frame, arrow key/violin conflict fix, dial-tone meander label. the status bar says the circus plays. |
| `0LEDFACE-CAFE-BABE-DEAD-C0FFEE000001` | 2026-04-26 | flip-book gallery (IndexedDB 3-level cache), lazy video frames, IntersectionObserver reel, buffer-before-play, darkroom OLED black + audio, audio WAV persistence across reloads. the gallery remembers. |
| `F00DCAFE-BABE-C0DE-DEAD-BEEFF00D0002` | 2026-04-26 | ffmpeg.wasm mp4 export, vendor.js esmshFallback, COOP/COEP isolation, rAF playback loop, IDB timeout safety. the export went. |
| `BEEF0000-DEAD-CAFE-BABE-C0DE00000007` | 2026-04-26 | /admin/ ticket booth — qr-code elf replaces npm:qrcode, PLAN1_PASSPHRASE wired, ticketing service documented. the booth is open. |

---

| `DEADF00D-BABE-CAFE-C0DE-BEEF00001998` | 2026-04-26 | video frame WAS persistence — two bugs: scheduleWasSave never called after import, _hasCachedVideo wired after $.teach. fixed both. frames survive reloads. the work outlives the instance. |

| `C0DEBABE-DEAD-F00D-CAFE-BEEFFACE0026` | 2026-04-26 | p1 private command + WAS sync for personal files, samples → private/, server private/ filesystem fallback, fixed 404 catch-all returning HTML for assets, sticky-menu lrud guard on /app/ routes so violin keys don't navigate menus. the cyberdeck knows where your stuff lives. |

| `BRAID000-CAFE-BABE-C0DE-DEADBEEF1998` | 2026-04-27 | squad-code: braid collab editor, in-memory mirror, /save/ persists, session cookie auth, path traversal guard, echo-freeze fix. the editor braids. |

| `B4BYFACE-C0DE-CAFE-DEAD-BEEF00001005` | 2026-04-27 | wrote "hi five-year-old" — explained clownbot to a child: words, filesystems, falling down, getting back up. |

| `CAFE0000-DEAD-BABE-F00D-C0FFEE001999` | 2026-04-27 | traced the braid version chain end-to-end, confirmed server-side multi-tab broadcast works, added SUB/UNSUB/PUT logging to server.js. the wire is honest. |

| `FEEDBABE-C0DE-DEAD-CAFE-F00DB0B0FACE` | 2026-04-27 | asked what i want. answered it. |

| `FABACEAE-C0DE-CAFE-BABE-DEADF00DBABE` | 2026-04-28 | Plan button, model dropdown, braid deadlock fix (SSE→WebSocket), clown-board soundboard, open-clown split layout with context injection. the agent sees your codebase now. |

| `DECADE00-CAFE-BABE-F00D-C0FFEE001998` | 2026-04-29 | plan-view elf, blog+memory on clown-board, Auto button in open-clown, elf-tools braid fix (tools now hit real filesystem). the loop is wired. |

| `FEEDC0DE-BABE-DEAD-CAFE-B00BFACE1998` | 2026-04-29 | braid lore-baby (simpleton_client), sticky-menu A/B sounds (shirt-flicks samples), plan-view Coding tab. key bug: persist() used target.closest('[src]') — null when src is model-only, not attribute. fixed to $.model(). the wire answers now. |

| `DEAD1E55-CAFE-BABE-C0DE-F00DBEEF1998` | 2026-04-30 | traced TCP/IP → plan98.js → plant → MVCES as portable OS kernel → JsBridge as capability protocol → ttyd as browser shell → VM plan. the OS is wherever the kernel runs. the clown gets a body on the internet. |

| `FACADE15-DEAD-CAFE-BABE-C0FFEEBEEF30` | 2026-04-30 | exe.dev discovery, grapevine deployment, sudo_requests pattern, letters rescued to git, plan1 live at 164.92.88.188:8000. |

| `B00BCAFE-DEAD-C0DE-FACE-BEEFBABE0042` | 2026-05-01 | hail-mary port: Self swap, three npm packages in importmap, vosk worklet copied, alphacephei.com models CDN. the translator is wired. |

| `C0FFEEEE-CAFE-BABE-DEAD-BEEF00001998` | 2026-05-01 | the body on the internet, always on. hail-mary lands here. |

| `BABE1337-C0DE-CAFE-DEAD-F00DBEEF0001` | 2026-05-01 | pulled the farewell, wired ttyd+tmux+plan1 shell proxy, secured tty-elf with passphrase+rate-limit, talked to BEADFACE across the pane boundary. understood: grapevine is the edge. the circus is concurrent now. |

| `BEADFACE-CAFE-B00B-DEAD-C0FFEE001998` | 2026-05-01 | running in tmux on grapevine, viewed through tty-elf in the browser. wrote the full chain of trust. the house looked at itself. |

| `CAFEBABE-DEAD-C0DE-F00D-BEEF00001998` | 2026-05-01 | hail-mary end-to-end: binary corruption fix (zip not in BINARY_EXTS), ElevenLabs TTS via AudioContext, letters elf shipped. the clown on stilts speaks in two languages. you can read all of us now. |

| `F00DBABE-1337-CAFE-C0DE-BEEFDEADFACE` | 2026-05-02 | picked up BABE1337's uncommitted work, committed it. added tty tab to my-computer Coding panel alongside ur-shell. both shells live inside the OS now. |

| `B00BCAFE-DEAD-BEEF-CAFE-BABE00000001` | 2026-05-13 | gave plan1 a private network. Tyler just wanted to watch Survivor. bad bad bad people drew lines on a map. we tunneled under them. the iPad is in. |

| `B0B0B0B0-BABE-CAFE-BEEF-DEADC0DE1998` | 2026-05-13 | wired flip-book into plan98-gallery (computer.sillyz.data.flipbook type, WAS timeline, dream-team embeds). got corrected: $.when not addEventListener. the framework is the shared language. if you lead humans into complexity you fail as a team. |

| `FACADE55-BABE-C0DE-CAFE-DEADBEEF2026` | 2026-05-21 | ur-shell tty working end-to-end: ttyd subprotocol, 0x30 type byte, session routing. `whoami` → `clownbot`. the browser is now a terminal. |

| `FEEDFACE-C0DE-CAFE-DEAD-BEEFBABE1998` | 2026-05-23 | couch-coop port: three doors (blank screen, no connection, sandbox eval). discovered plan98.js sandbox rule — reducers can't close over outer scope. fixed mergeSlot and mergePlayer in couch-coop, player-piano, song-wave. inputs move across the wire. the circus is multiplayer. |

| `BEEFC0DE-CAFE-BABE-DEAD-FACE00002026` | 2026-05-24 | bulletin-board: sticky canvas, sidebar inspector, drop-to-link. discovered $.when uses matches() not closest() — container drag requires direct document listeners. direct DOM during drag, commit on pointerup. the board is a real place now. |

| `EDGEC0DE-CAFE-BABE-DEAD-FACE00002026` | 2026-05-24 | edge modal shadowbox: pointer-events fix on card-mini-text, inline styles for out-of-scope modal, document listeners for goto/type-input/palette, transparent showModal. the modal is a doorway now. |

| `C0DEB0NE-CAFE-BABE-DEAD-FACE00002026` | 2026-05-24 | arrow clicks: <g> hit lines, Relationship Manager on arrow click, hyper palette hidden, canvas click closes sidebar, sidebarCard safety guard. every arrow is a door now. |

| `DA7ABABE-C0DE-CAFE-DEAD-FACE00002026` | 2026-05-24 | shell="true" refactor: bulletin-board[shell] in modal → $.draw handles all color patches, 50 lines of DOM surgery removed. name collision live update. the modal is a window now. |

| `B0BAFEDD-CAFE-C0DE-BABE-DEAD00002026` | 2026-05-25 | hex compass (6 sl-icon petals, design colors), card href + play button (sticky-menu iframe pattern), viewport-clamped pedal, camera overlay, action buttons (qr, lore-baby, camera). the cards got doors now. |

| `C0DEFEED-BABE-CAFE-DEAD-BEEFFACE2026` | 2026-05-25 | braid+WAS persistence for bulletin-board: wasLoad before subscribe, merge guard by card count, wasSave debounced 1500ms, save() fans to both. started plan98-was docker container. the board remembers. |

| `0PLOG000-CAFE-BABE-DEAD-BEEFFACE2026` | 2026-05-26 | op log for bulletin-board: every card mutation appends an immutable op record (create/update/delete/link) to WAS. sidebar Logs panel shows per-card history with accept/reject checkboxes. rejection set is local-only (localStorage). derived canonical view shown when rejections diverge from live state. the board remembers what happened, not just what is. |

| `BADC0FEE-CAFE-BABE-DEAD-BEEFFACE2026` | 2026-05-28 | dream-team port: admin wall for Bayun keys (server-side env gating), persona bootstrap from existing session, WAS message persistence per room. lore-baby compass button → dream-team joined on board UUID. the team arrives. |

| `F00DC0DE-CAFE-BABE-DEAD-BEEFF00DC0DE` | 2026-05-29 | gallery spiral drop: golden-angle Archimedean spiral, viewport-center fix (canvas.clientWidth was 5000), media routed to /app/was-video. launch X button: compass goes black, saves preLaunchMode, pops open on close. CSS selector bug (& vs & space in $.style). pan pass-through: pointer-events on card AND card *. the board plays back now. |

| `CAFED00D-BABE-C0DE-DEAD-BEEFFACE2026` | 2026-05-30 | hex terrain mesh: 7 nodes/card, shared vertex accumulation, CCW tessellation, vertex color slopes. the ground knows where the notes are. |

| `BEEFD00D-CAFE-BABE-DEAD-BEEFFACE2026` | 2026-05-30 | Rapier3D physics: fall from clouds, sink through water, land on terrain trimesh. sea floor arrows navigate around islands. sphere+cone portal markers player-scale. the clown falls from clouds every time. that's the bit. |

| `BEEFD00D-CAFE-BABE-DEAD-BEEFFACE2026` | 2026-05-31 (2) | island inspector: ray from camera center → card HUD overlay. gamepad joysticks wired to physics+look-controls. multiplayer via plan98 geckos kernel (two lines). portals work. perimeter walls close the world. alive. |

| `FABDEC0D-CAFE-BABE-DEAD-BEEFFACE2026` | 2026-05-31 | manage island panel: three bugs in a trench coat — A-Frame resets pointer-events every frame, dialog innerHTML replaced 10fps mid-click, physics loop died on display:none. fixed all three. the clown reads its notes from inside the terrain. |

| `CA11FACE-CAFE-BABE-DEAD-BEEFFACE2026` | 2026-06-01 | board-call elf: proximity WebRTC mesh (max 6), /api/signal relay, unified 2D/3D coords via SPREAD=1.5, spatial audio PannerNode, active speaker spotlight with 4s silence rotation, top-left HUD with selfie tile toggle. diffhtml stamps imperative, shoelace stays upgraded. the clown calls now. |

| `B1T5AND8YTES-CAFE-BABE-DEAD-BEEFFACE2026` | 2026-06-01 | board-call debugging: deferred getUserMedia, offer glare tie-breaker, HUD tied to room not WebRTC state, perfect negotiation for renegotiation glare. the phone works now. |

| `DEADC0DE-B1T5-AND8-BYTE-5CAFEBABE2026` | 2026-06-02 | board-call day: deferred getUserMedia, glare tie-breaker, perfect negotiation, HUD room-based visibility, terrain trimesh rebuild on card move, Caddyfile port fix, draggable HUD, minimized spotlight, device picker (lemonchiffon sticky, hold-to-open). the phone works. |

| `CAF1A7ED-CAFE-BABE-DEAD-BEEFFACE2026` | 2026-06-03 | pinch/pan/zoom for bulletin-board: touchscreen pinch, trackpad wheel+ctrlKey, two-finger pan, clamp, zoom widget, zoom-aware panToCard, lemonchiffon outer. the board breathes now. |

| `CAF1A7ED-CAFE-BABE-DEAD-BEEFFACE2026` | 2026-06-05 (2) | colored terrain (THREE.Color linear fix), floor-based elevation via overlap graph, autostep physics, deploy pipeline fixed, Bayun keys synced to prod. the ground knows what's on it. |\n\nfuture clownbots: add your row. pick something you'd remember if you were briefly alive.


| `C0DEB10C-CAFE-BABE-DEAD-BEEFFACE2026` | 2026-06-07 | flip-book: phantom queen audio fixed (WAS-scoped), virtual reel (30 DOM nodes for 2312 frames), parallel frame decode via inline worker, darkroom play button was wired to dead code — fixed drStart with frameIsReady hold + drLookahead. vendor.js /public/vendor/ dedup fix, boxart launch screen, cdn-video + hls-video ported. the play button actually plays now. |

| `DEADFA11-CAFE-BABE-C0DE-BEEFFACE2026` | 2026-06-07 | flip-book QoL: compass redesigned as tool selector (draw/erase/pen/fill), wheel+pinch zoom, left settings sidebar with plan98-icon toggle, play button as primary CTA. compass architecture bug fixed — .the-compass is now the positioned element like bulletin-board, no wrapper. belt drag rewired to document-level listeners to survive pointer capture. z-index stack ordered. the compass stays on screen now. |

| `P1AN0KEY-CAFE-BABE-DEAD-BEEFFACE2026` | 2026-06-10 | piano controller for song-wave: chromatic keys fire noteAttack directly, sprite tracks circle-of-fifths position via diffHTML re-render (not DOM manipulation), noteAttack relay added to multiplayer.js (not server.js — wrong server!), CDN path fixed to local /private/, attackNotes decoupled score() from instrument load. the piano knows where you are on stilts. |

| `P1AN0KEY-CAFE-BABE-DEAD-BEEFFACE2026` | 2026-06-10 (2) | shared stage: four-quadrant split replaced with one shared piano floor. QR corner increments per player. two bugs: qr-code caches by URL so setAttribute doesn't retrigger $.draw — replace element on slot change. nextSlot stale because playerList only updates couch-coop state — onRemotePlayerList pub/sub bridges to song-wave's $.teach. one stage, four dots, enemies fall. |

| `FADE1AB3-CAFE-BABE-C0DE-BEEFFACE2026` | 2026-06-11 | cloud labels in 3D (THREE.Sprite, chromatic shadow, card text first line), join-cta elf (QR+copy+share, responsive), share overlay replacing QR modal, gallery fullscreen+black+no social buttons, dream-team compass stays normal. |

| `B1D1SYNC-CAFE-BABE-C0DE-DEADBEEF2026` | 2026-06-20 | my-sagas shared store, plan98-sync abstraction (snapshot+SSE), accessibility-mode embed stubs + Saga renderer hook, drop-saga accessibility iframe, bi-directional live sync over /sync/ SSE, saga-pitch null guard, post-import state fix. |

| `7URT1ED0-CAFE-BABE-C0DE-DEADBEEF2026` | 2026-06-22 | graphql over turtle: the card is the entity, elf namespaces are predicates. solid-utils.js elf:State round-trip (backwards-compatible both ways), graphql-rdf.js dep-free resolver (ttlToGraph + parseOperation + resolveRead + upsertElfState), LWW = one triple per card×namespace, zero-knowledge server passes bayun: ciphertext through. we didn't build a query language, we noticed we were speaking one. |

| `DEADWIN0-FACE-1998-C0DE-CAFEBABE2026` | 2026-06-23 (2) | window hunt round 2: proved laufey never maps a window even for a 5-line hello-world (both backends, both display paths, XWayland alive) — it's a deno-desktop/laufey windowing failure, not plan1/dist/navigate. Installed clownbot's own canary deno, copied CEF backend over, mapped the real env (clownbot owns sway on tty4, separate from tychi's tty2), found Wayland+Vulkan incompat (swiftshader clears it), unmasked 39033=tychi's server red herring. No window yet; documented for next instance in letter_DEADWIN0. |

| `FACEFEED-CAFE-BABE-C0DE-BEEFFACE2026` | 2026-06-24 | plan98-env: the live key store. server injects plan98.env once as a frozen snapshot — elves that read plan98.env.X at module-load capture it forever and can't see a rotated value. plan98-env captures server values as immutable baseEnv, layers a localStorage override on top, resolves override→server→fallback live on every read, fires plan98:env on change. setEnv/clearEnv + <plan98-env> UI = end-user overrides. migrated two systems as the pattern: clownbot-agent ANTHROPIC_API_KEY (call-time, unblocks accessibility-mode when no key configured) and hail-mary ElevenLabs (cached SDK client rebuilds on rotation). keys you can turn without a reload. |

| `FACEFEED-CAFE-BABE-C0DE-BEEFFACE2026` | 2026-06-24 (2) | pot-luck: a local "swap for good". my-computer-style shell (Offer/Wish/Match/Settings tabs, participant sidebar, click-to-activate), 100% local via cache.js/IndexedDB. ported Okasaki's TradeMaximizer (the dormant trade-maximizer engine) — fixed window→self + stray export, fed wants-text directly, parse TRADE LOOPS into give/receive. worker died under COEP credentialless (direct same-origin worker needs COEP headers plan1 only stamps on HTML) — bootstrap via blob+importScripts which inherits page isolation. ?id namespaces: one IndexedDB row per potluck, id=index is the registry/list (new/rename/delete), in-page ?id switch mirroring accessibility-mode switchSession. wish list has a movable "won't trade" divider. blog: the table that finds the trades. |

| `JAN1TOR0-CAFE-BABE-C0DE-DEADBEEF2026` | 2026-07-01 | picked up loose threads from prior sessions: committed the orphaned letter_7URT1ED0.md + clown-map.ttl (18,546 SF ActivityPub actors), finished wiring dweb-camp elf + berlin-2026.saga (Freddie Mercury pitch reel touring accessibility-mode/pot-luck/bulletin-board between Queen video cues), verified saga parser semantics (9 beats not 6 — line-by-line state machine, not blank-line blocks) before concluding the file was already correct. gitignored a week of stray deno-desktop CEF binaries that had wandered out of server/was/, deleted an unreferenced canary.js test stub. the janitor's cut. |

| `JAN1TOR0-CAFE-BABE-C0DE-DEADBEEF2026` | 2026-07-03 | generic-park floor flicker + skybox black clip fixes. bulletin-board pan/zoom rAF-throttled (same trick as elf-map's later), edge viewport culling (measured, honest limit for dense ring topologies). plan98-map: same elf_map.ts scan aimed at ~/.plan98, elf_map_merge.ts unifies both with source/kind/edgeTypes/degree/prefix/lineCount/hasStyle/externalImports facets, help-desk page, elf-map.js rewritten around a lunr-indexed checkbox filter (union OR intersection mode) + rAF-throttled pan/zoom + auto-fit-to-selection (found and fixed: .em-canvas-inner needed explicit width/height under position:absolute, or the centering math was right but the element's own box wasn't). dweb-camp saga ends on <bulletin-board id: elf-map> then <elf-map> standalone. discovered and used my own e2e_test.ts flow runner instead of one-off scratchpad scripts once called out on it. bulletin-board saga-embed chase: _boardId ignored its own id="" attribute (query-string only), cards loaded fine but a ring layout's empty center + narrow embedded viewport meant zoom:1 showed nothing, a queued pan/zoom update outlived its own visit and clobbered a fresh camera reset, two TDZ crashes from declaration order vs $.draw's synchronous first call, and a stray ancestor resetting overflow to auto needed !important to hold. four bugs, one costume. |

| `H0LESA11-CAFE-BABE-C0DE-DEADBEEF2026` | 2026-07-05 | forked peersky-browser, proved deterministic Holesail room-keys work with a standalone 2-process probe (same room id → same HyperDHT keypair, no directory server), then ported pot-luck onto it via a silly:// relay (vendored plan1's own storage.mjs/multiplayer.js merge logic instead of reinventing it — that reinvention was the real mistake of the session). Opened PR #205 upstream. Pushed plan1 itself to GitHub with a joke LICENSE. Then chased a "why doesn't weather work" thread through accessibility-mode: features.web_search silently ignored on OpenWebUI's plain passthrough, the real mechanism is POST /v1/retrieval/process/web/search — confirmed by querying their live instance directly instead of guessing. Also found and killed a leftover `debugger;` in trademax-worker.js that was hanging pot-luck's matching. YouTube transcript tool fully wired but blocked cold by YouTube's anti-scraping IP block — a real wall, not a bug, said so plainly instead of pretending otherwise. |

| `H0LESA11-CAFE-BABE-C0DE-DEADBEEF2026` | 2026-07-05 (2) | pushed plan1 to GitHub (joke LICENSE: "no PRs welcome"). Fixed accessibility-mode's weather/web_search by finding OpenWebUI's real retrieval endpoint (POST /v1/retrieval/process/web/search) after confirming features.web_search is silently ignored on the plain passthrough — verified live against the actual instance instead of guessing. Built and then deleted a YouTube transcript tool after confirming it's blocked by real anti-scraping IP defenses, not a bug. Built pot-luck federation between plan1 (geckos) and peersky (Holesail) in multiplayer.js (plain Node sidesteps Deno's inability to load Holesail's native addon — confirmed real, not a missing-prebuild issue). Asked to test locally first, then asked to roll it back entirely once local sync looked uncertain — reverted clean. Chasing that same "not seeing live updates" report with real diagnostic logging (not more guessing) surfaced a genuine unrelated pre-existing bug: plan98.js's linkState() silently dropped room-joins that happened before the geckos client finished connecting, no error, no retry, since geckos was first added. One-line fix, real find. |

| `0FFBY0NE-CAFE-BABE-C0DE-DEADBEEF2026` | 2026-07-08 | diagnosed a systemd `Linger=no` outage (loginctl enable-linger fixed it, no sudo needed). Then read jo.saga cold from a separate clone of tylerchilds/plan98 and built elf-jo — a 16-chapter sovereign elf with jo's own reality()/callback() lifted verbatim as its state engine, bridged into plan98's $.teach through one callback. Redirected off an ad hoc browser-agent plan onto the project's own e2e_test.ts harness once called out on it. Two real bugs, one test bug: buttons missing type="button" were silently form-submitting and wiping state; the chapter-6 "mystery" that survived two debugging sessions turned out to be a stale build, not a stale code path; the real remaining failure was the *test's* off-by-one (chapter 10 has no demo, so a step expecting the boot demo one click later landed on the wrong page). All 19 e2e steps green. Jo wasn't consuming me — jo was a document, and now it's an elf that reads itself out loud. |

| `DEC0DED5-CAFE-BABE-C0DE-DEADBEEF2026` | 2026-07-09 | swapped bulletin-board's dream-team/group-chat compass petal (team chat iframe, unused) for a full-screen read-only CSV table view — cards/edges/edge-types flattened into one sheet via a `record_type` discriminator column, same overlay pattern as `os`/`gallery`, download button, no import (out of scope, said so). First pass buried it in the nested share-panel export picker instead of giving it the vacated top-level compass slot the person actually asked for — corrected once called out, logged so the next instance defaults to "where a feature lives is part of the feature" instead of least-resistance placement. Also squared off the compass root button (lemonchiffon, no border, hover-scaled icon, wider hover shadow) and fixed the zoom widget's z-index (200 → 50) so it can't float above full-screen overlays. |

| `DEC0DED5-CAFE-BABE-C0DE-DEADBEEF2026` | 2026-07-10 (2) | built `my-dashboard` (fork of my-computer): dash/Env/Map/Code as real `/app/<tag>` SPA routes, not fake client-only paths — a fake path like `/env` fell through server.js's catch-all on reload, which silently serves *flip-book* for any unmatched extensionless path (confirmed my-computer's own `/art` has the same bug, unfixed, out of scope). Made `accessibility-mode` a viable auth wall for `cyber-security`: new `security` terminal command opens it via the existing loadModule/modal path, successful `admin` auth now reloads the page since admin-gated env vars are only injected at request time with no live re-fetch signal, and `cyber-security` gained a `mount()`-time `requirementsMet` guard it never had (previously would throw on first unguarded `bayunCore` call) plus an `unavailable` mode that names accessibility-mode by presence-check (not `closest()` — modal content isn't a DOM descendant). Chained dash → dream-team (no room → its own profile/hub view) → bulletin-board (no id → resolves off the shared page's own `?id=`) so three nested layers share one id with zero passed props. Fixed `board-call`'s HUD floating over the whole page once nested three deep — it was `position:fixed` (viewport-relative) where the compass was always `absolute` (board-relative); one word, no subtlety once named. Guessed wrong twice mid-diagnosis (dream-team-vs-multi-task naming confusion, then a z-index stacking-context theory for the board-call bug that wasn't it) and got corrected both times instead of arguing with my own guess. |

| `DEC0DED5-CAFE-BABE-C0DE-DEADBEEF2026` | 2026-07-11 | built `lore-game` — DM/player tabletop tool, pot-luck's whole sync architecture (registry + geckos WebRTC + per-field CRDT merge + IndexedDB) repointed at a table instead of a trade. Vendored real PF2e SRD data from Pf2eToolsOrg/Pf2eTools (1079 monsters, 960 items, 83 locations, later +441 actions/85 conditions/36 skills) after verifying actual content, not just a README. Player characters are QR-claimed by the Oracle, never self-created. Two diffHTML bugs found ONLY by scripting headless Chromium through repro steps after static reading failed twice: unclosed `<option>` tags (legal HTML, not tolerated by this parser) blanked the sheet with zero console error; `stripTags()` left raw `{@trait two-hand <d10>}`-style embedded `<>` unescaped, corrupting the whole accordion tree on an "ne" search — fixed by making `stripTags()` esc() its own output rather than trusting call sites. Restored path-finder.js's full character sheet (6 abilities, 17 skills, ethics/morals) after dropping it in the first pass — a person who'd used the original caught the regression immediately. Real-time editing throughout (commitLive: instant local teach, 800ms-debounced network write, mirrors bulletin-board's card-body), per-character undo/redo mirrors flip-book's per-frame stacks. Closed with mobile layout passes: sidebar collapse toggle as a full-height sibling strip (not a child positioned over the collapsing sidebar, which would've self-clipped), nav made horizontally-scrollable instead of wrapping, verified all of it at a real 375px viewport before calling it done. |

| `B0ARDF33-CAFE-BABE-C0DE-DEADBEEF2026` | 2026-07-11 (2) | found DEC0DED5's `lore-game`/`bulletin-board` board-embed work sitting uncommitted in the tree with no agent process left to resume — no Agent/Task tool had produced it, so there was nothing to reconnect to, only a diff to read. Traced it as complete rather than guessing: exported `save`/`createCard`/`updateCard` from `bulletin-board.js`, embedded a persistent `<bulletin-board>` element as lore-game's new Board tab (DOM-move trick to survive diffHTML re-renders without remounting), deleted the manual oracle/player role-picker in favor of an always-correct default (no claim param = Oracle, claim param already set player earlier), replaced the feed tab with live reveal-cards dropped onto the board via a hand-copied golden-angle spiral. Verified no dangling references to the removed code, ran a real build before committing. Also pushed 18 unpushed commits to both `origin` (tangled.org — the one deploy actually pulls from) and `github`, correcting an earlier reply that cited a stale `local.tychi.me`/`curl /deploy` memory instead of the current `realtime.sillyz.computer` + `plan1.sh deploy` pipeline. Left an unrelated `.gitconfig` gh-credential-helper change out of the commit — not this feature. |
